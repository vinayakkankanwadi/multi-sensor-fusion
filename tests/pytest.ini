[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: regression tests that wait for the nodes poll cycle (use -m "not slow" to skip)
filterwarnings =
    ignore::DeprecationWarning:google.protobuf
